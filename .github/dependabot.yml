version: 2
updates:
  # Workflow actions are pinned by commit SHA; this keeps the pins current and reviewable.
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
